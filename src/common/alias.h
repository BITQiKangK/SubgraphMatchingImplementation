#pragma once

#include <cstdint>

using ui = unsigned int;
using VertexLabel = ui;
using EdgeLabel = ui;
using VertexID = ui;
using EdgeID = ui;

#define INVALID_EDGE_LABEL UINT32_MAX
#define INVALID_VERTEX_LABEL UINT32_MAX
#define INVALID_VERTEX_ID  UINT32_MAX
#define INVALID_EDGE_ID    UINT32_MAX
